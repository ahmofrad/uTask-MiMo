apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "taskapp.fullname" . }}-migrate-{{ .Release.Revision }}
  labels:
    {{- include "taskapp.labels" . | nindent 4 }}
    app.kubernetes.io/component: migrate
spec:
  ttlSecondsAfterFinished: 3600
  backoffLimit: 6
  activeDeadlineSeconds: 900
  template:
    metadata:
      labels:
        {{- include "taskapp.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: migrate
    spec:
      restartPolicy: Never
      containers:
        - name: migrate
          image: "{{ .Values.app.image }}:{{ .Values.app.tag }}"
          imagePullPolicy: IfNotPresent
          command: ["sh", "-c"]
          args:
            - npx tsx scripts/migration-preflight.ts --allow-pending && npx prisma migrate deploy && npx tsx scripts/migration-preflight.ts
          env:
            - name: RUN_MIGRATIONS
              value: "false"
          envFrom:
            - configMapRef:
                name: {{ include "taskapp.fullname" . }}
            - secretRef:
                name: {{ include "taskapp.fullname" . }}
