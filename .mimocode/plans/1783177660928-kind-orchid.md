# Plan: Default Seed vs Sample Data Seed

## Goal
- `pnpm db:seed` creates ONLY admin@taskapp.dev (password: password) — clean first-run
- `pnpm db:sample` (new command) creates all 7 users + departments + projects + tasks + tags etc.

## Files to modify

### 1. `prisma/seed.ts` — rewrite as minimal seed
- Keep `hashPassword()` helper
- Remove all users except admin@taskapp.dev
- Remove all departments, projects, tasks, tags, comments, watchers
- Create only: admin user with `owner` global role (full permissions)
- Log the credentials at the end: `console.log("Admin: admin@taskapp.dev / password")`

### 2. `prisma/seed-sample.ts` — new file for sample data
- Move ALL the current seed content here: all 7 users, departments, projects, tasks, custom fields, tags, comments, watchers, role assignments
- Idempotent (uses upsert where possible, like current seed)

### 3. `package.json` — add new script
- `"db:sample": "tsx prisma/seed-sample.ts"`

### 4. `README.md` — update quick start
- Change step 4 to just `pnpm db:seed`
- Add note that `pnpm db:sample` is available for demo data

### 5. `INSTALL.md` — update first login section
- Change `pnpm db:seed` to just create admin
- Add `pnpm db:sample` as optional for demo data

## Verification
- `pnpm db:seed` creates only admin@taskapp.dev with owner role
- `pnpm db:sample` creates all 7 users + departments + projects + tasks
- `pnpm db:sample` is idempotent (safe to run multiple times)
