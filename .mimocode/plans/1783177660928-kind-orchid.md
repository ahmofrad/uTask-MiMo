# Plan: Fix Gantt Chart Jalali Dates + Task Name Styling

## Changes

### `src/components/task/gantt-chart.tsx`
1. Replace `toLocaleString("default", { month: "short", year: "numeric" })` with `toJalaliMonthYear()` using `toJalali` from `@/lib/date/jalali`
2. Replace `formatDay()` with `toJalaliLabel()` using `toJalali` + `getMonthName`
3. Import `useLocale` and `useFormattedDate` for locale-aware rendering
4. Task name: `line-clamp-2 leading-snug` for truncation, add priority badge + date range below name
5. Task bars: add `shadow-sm` for depth
6. Use `shortDate()` for start/end dates in task name row
7. Replace hardcoded legend text with `t("status.${status}")` translations

## Verification
- Gantt chart axis shows Jalali month names (فروردین, اردیبهشت, etc.)
- Task names are truncated to 2 lines with priority + date range below
- Status legend uses translated labels
